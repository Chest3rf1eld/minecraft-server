# Architecture

The canonical architecture is maintained in `../minecraft-server-architecture.md`.

This directory file exists as the stable documentation entrypoint expected by the repository layout. Keep design changes in the canonical architecture document first, then update `SPEC.md` and `PLAN.md` if the implementation contract or phase plan changes.
